---
import FluidGrid from '@components/FluidGrid.astro';
import VimeoCard, { type Props as CardProps } from './VimeoCard.astro';

interface Props {
  videos: CardProps[];
}
---

<FluidGrid minColumnWidth="18rem">
  {Astro.props.videos.map((video) => <VimeoCard {...video} />)}
</FluidGrid>
